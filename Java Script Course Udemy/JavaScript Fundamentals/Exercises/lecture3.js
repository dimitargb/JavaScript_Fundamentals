// We use keyword let when we later whant to cahneg or reasing a variable

let age = 30;
age = 50;

// We use the keyword const to declare a varable that are not supposed to change at any point in a future.

const gun = "Marakov";
//gun = "Beretta";
//console.log(gun); -> it gives an error becuse we use cont and we can not chnage the variable;

// we can not declare empty const variables:
//const toy;
