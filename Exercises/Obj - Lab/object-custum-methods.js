let info = {
    firstName:'Dimitar',
    secondName:'Damianov',
    eyeColor: 'black',
    age: 50,
    isMale:true,
}
let objectProperties = Object.keys(info); // това е масив
let objectVelues = Object.values(info); // това е масив
let objectEntries = Object.entries(info);// това е масив